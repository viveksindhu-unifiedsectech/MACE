export interface AdminUser { user_id: string; email: string; full_name: string; role: string; tenant_id: string; tenant_name: string; jurisdiction: string; weight_profile: string; plan: string }
export interface User { id: string; email: string; full_name: string; role: string; is_active: boolean; is_verified: boolean; mfa_enabled: boolean; last_login_at: string | null; created_at: string }
export interface APIKey { id: string; name: string; prefix: string; scopes: string[]; is_active: boolean; last_used_at: string | null }
export interface Connector { id: string; type: string; name: string; status: string; sync_enabled: boolean; sync_interval_minutes: number; last_sync_at: string | null; last_sync_count: number; error_message: string | null; provides_assets: boolean; provides_vulns: boolean; provides_events: boolean }
export interface Subscription { id?: string; plan_name: string; status: string; asset_limit: number; assets_used: number; price_per_asset_usd: number | null; current_period_start: string | null; current_period_end: string | null; trial_end: string | null; features: Record<string, unknown> }
export interface AuditEntry { id: string; action: string; user_email: string | null; resource_type: string | null; resource_id: string | null; success: boolean; ip_address: string | null; created_at: string }
export interface TenantInfo { id: string; name: string; slug: string; plan: string; jurisdiction: string; weight_profile: string; sector: string; data_residency: string; asset_limit: number; is_fedramp: boolean; is_hipaa_baa: boolean; soc2_compliant: boolean; cdcs_alert_threshold: number; rea_cdcs_threshold: number; enabled_frameworks: string[]; mace_stats: Record<string, unknown>; created_at: string }
export interface PlatformStats { assets: { total: number; limit: number }; incidents: { open: number; critical: number }; vulnerabilities: { open: number }; engine: Record<string, unknown>; regulatory_calendar: unknown[] }
